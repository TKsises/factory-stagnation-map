# このリポジトリで実際に踏んだ地雷

一般論は書かない。**ここで実際に起きたことだけ**を記録する。

## データの地雷（Smart Craft の工程実績CSV）

- **「実績の開始」の列名は `作業開始日時` であって、「実績」の語を含まない。**
  同じCSVに `開始予定日時` が並び、しかも**列の並びは予定の方が先**。
  → 「"実績"を含む列を探す」は1件もヒットしない。「"開始"を含む列」だと予定を掴む。
  → `mapping.ts` の `forbid: ['予定']` が唯一の防波堤。**このガードを外すとテストが落ちる**
  （確認済み。`guessMapping ― 「予定」を含む列は実績候補にしない` が落ちる）。

- **単位の列が無い。** 工程実績CSVは品目コードまでしか持たず、単位は品目マスタ側にある。
  → 単位は `Config.costs[品目コード].unit` に手入力で持つ。CSVから取ろうとしない。

- **`工程順` は必ず取れる。** `ProcessResult` が `process_order` に delegate しているため。
  時刻順に並べ替える経路は保険であって主経路ではない。

- **日時の書式が2種類ある。** エクスポートは `2026/08/17 14:30:00`（0埋め・秒あり）、
  取り込みテンプレートは `2023/1/1 10:00`（0埋め無し・秒なし）。両方受ける。
  `new Date(文字列)` に渡さない（`2023/1/1` の解釈が環境で揺れる）。

- **文字コードは顧客が選べる。** エクスポート画面に UTF-8 / Shift_JIS の選択肢が実在する。
  Shift_JIS 対応は「念のため」ではなく必須。

根拠の行番号は `docs/phase0-data-findings.md` にある。

## 検証まわりで自分が踏んだもの

- **合成した drop イベントは、ドロップゾーン**そのもの**に投げる。**
  `document.querySelectorAll('div')` の `find` は**最も外側**の一致を返すので、
  祖先要素に投げてしまい、イベントが内側のハンドラに届かず「何も起きない」になった。
  → `document.querySelector('input[type=file]').parentElement` を掴む。

- **スクリーンショットが撮れない環境がある**（Browser ペインが表示されていないと
  コンポジットされず 5 秒でタイムアウトする）。そのときは DOM の実測値で確かめる:
  `getComputedStyle(th).color` で色を、`innerText` で数字を読む。

- **文字化けの確認は目視に頼らない。** `/[�]/.test(document.body.innerText)` で
  置換文字の有無を数値的に確かめる。

- **`document.elementFromPoint` に送る前に `scrollIntoView` する。**
  対象が画面外だと `null` が返り、`Cannot read properties of null` で落ちる。
  スクリーンショットが撮れない環境では `computer` の座標クリックも使えない
  （事前の screenshot が必須なため）。DOM 経由で送るしかない。

- **仕込んだ異常データが本当に効いているかを、出力の件数で確かめる。**
  「工程の重なり」を仕込んだつもりで開始を5時間前倒ししていたが、
  元の滞留が40〜190時間あるので重なりにならず、**0件のまま**だった。
  前工程の終了時刻を持ち回って `prevEnd - 3h` にして初めて63件出た。

## 性能（実データ規模で計測。10件で測っても意味が無い）

11,885行 / 2,377ロット / 3.1MB で、`MutationObserver` で入力→DOM反映まで測った値:

| 操作 | 中央値 |
| --- | --- |
| CSV解析 | 91〜126 ms |
| 集計（buildLots→computeGaps→computeMetrics） | 80〜89 ms |
| 列マッピングの変更（全再集計が走る） | 230 ms |
| 山のクリック（再集計は走らない） | 76 ms |

クリックが 76ms かかるのは、`analysis` のメモ化は効いていても
**親が JSX 全体を組み立て直す**ため（プレビュー表が33列×20行ある）。
先行実装と同じ構図なので、重くなったらメモ化ではなくコンポーネント分割で対処する。

## レビューで見つかった「0にしない」の抜け道（すべて修正済み・回帰テストあり）

`computeGaps` 側で「除外を0にしない」を守っていても、**集計側で `?? 0` として復活していた**。
ルールは1箇所で守れば済むものではなく、`?? 0` を書くたびに疑うこと。

- **除外された工程間があるロットを、リードタイムの母数に入れていた。**
  分子（滞留）だけ欠けて分母は満額残るので、滞留率が実際より小さく出る。
  実測で 97.9% → 30.3% まで薄まった。しかも品質パネルは「1件から算出」と書いていた
  （＝表示している件数と、率を割っている件数が食い違っていた）。
  → `computeGaps` が `completeLotIds` を返し、`computeMetrics` はその集合だけを母数にする。
- **標準時間が欠けた工程を0分として合算していた。** 4工程中2工程分が黙って消えていた。
  → 1工程でも欠けていたら、そのロットの正味加工時間は出さない。
  併せて `合計標準時間`（標準時間＋段取り）を優先するようにした（段取りが丸ごと落ちていた）。
- **`headers.indexOf('')` が実在の列を掴んでいた。** 未割当は `''` なので、
  Excel由来の末尾カンマで空の列名ができると、工程順の列が無いのに「有る」と誤認し、
  並べ替えが効かず行順のまま計算されて `P030→P010` のような架空の工程間が生まれていた。
  → 空の列名には `（名前の無い列N）` と名前を付け、かつ `columnIndex()` のガードを通す。
  **`columnIndex()` は正しいガードを持っていたのに、呼び出し元2箇所とも自前の `indexOf` を
  使っていて死にコードになっていた。** 書いたガードが使われているか確認すること。
- **サマリーが名指しした最悪の工程間が、地図に描かれないことがあった。**
  地図は「流れの上で隣り合う工程」しか描かないが、集計は工程飛ばしの遷移も拾うため。
  → 隣り合わない工程間は基準線の下に破線の弧として描く。数字を隠す方向で辻褄を合わせない。
- **「読めない値」を1行で2回数えて割合が200%になっていた**（開始と終了の両方が読めない行）。
- **推測しただけの列マッピングを即座に保存していた。** 次回「前回の対応づけ」として復活し、
  ユーザーが一度も確認していない推測が確定値に化けていた。
  → 保存するのはユーザーが画面で列を選んだときだけ。`config.mapping` と使用中の対応づけを分ける。

## 性能：カレンダーは1回だけ前処理する

`workingHoursBetween` が呼び出しごとに `Set` を作り直し、休業日ごとに文字列を分解して
`Date` を作っていた。工程間9,508か所で実測：

| 休業日 | 修正前 |
| --- | --- |
| 0件 | 59 ms |
| 90件（日本の製造業3年分の現実的な件数） | 862 ms |
| 300件 | 5,305 ms |

**休業日が空だから表に出ていなかっただけ**で、カレンダー編集UIを作った瞬間に発現する。
→ `prepareCalendar()` で休業日を日番号の昇順配列にし、区間内の件数は二分探索で数える。
`computeGaps` は前処理を1回だけ行い、以降は前処理済みの形を渡す。

なお最初の計測では 123ms しか出ず「指摘は誇張」と誤判断しかけた。
原因は検証データ側で、**工程間が1日程度だと `countWorkingDays` の「間に挟まる日」が無く、
休業日ループにそもそも入らない**ため。性能を測るときは実データの分布に合わせること。

## 金額まわりで決めたこと

- **単位の正は `Config.costs[品目コード].unit`。** CSVの単位列は `Lot.unitFromCsv` に入れて
  参考表示にとどめ、食い違うロットは件数として画面に出す。
  出所を2つ持つと kg と ピース を取り違え、「単位が違う数量を合計しない」が守れなくなる。
  そもそも Smart Craft の工程実績CSVに単位列は無い。
- **`Gap` に金額を持たせない。** `computeGaps` の責務は時間だけ。
  金額は `money.ts` の関数が唯一の計算元で、画面もPNGもドリルダウンもそこを通る。
  2箇所に代入できる形にすると必ず食い違う。
- **金額の母数は、リードタイム・滞留率と同じロット集合に揃える。**
  別々の集合で出すと「対象2,105件」と書いてある横に、違う件数の金額が並ぶ。
- **単価未設定の品目は0円として混ぜない。** 混ぜると凍結額が実際より小さく出る。
  画面には「これらは計算から外しているので、実際の凍結額はこれより大きくなります」と出す
  （＝**過小に出ている**ことを明示する。過大に見せない）。
- **短縮日数が0のときは試算を出さない**（`—` と表示）。元の値が0のときは % も出さない。

## 書き出し画像（PNG）

- **形と寸法は `render/mapShape.ts` だけが持つ。** 描画先は SVG（画面）と Canvas（書き出し）の
  2系統あり、それぞれで形を書くと片方だけ古くなる。
  先行実装では「画面では2本のパスで描いたアイコンが、書き出しでは1本で丸い塊」になった。
- **DOMを画像化しない。状態から Canvas に描き直す。** 外部ライブラリ不要で2倍解像度に出せる。
- **`renderProposalPng()` は dataURL を返すだけの純粋関数。** ダウンロードは呼び出し側の責務。
  こうしておくと、検証で dataURL を横取りして画面に貼るのが簡単になる。
- **脚注を省略しない。** 画像は口頭説明なしで渡るので、根拠が図の中に無いと決裁の場で止まる。
  脚注の文言は `buildFootnotes()` に閉じ込めてテストしてある（断定表現が無いことも含む）。

### 検証のしかた（実測済み）

1. `HTMLAnchorElement.prototype.click` を差し替えて dataURL を横取りする
2. `<img>` として全画面に貼り、**原寸（naturalWidth）で表示して**スクリーンショットを撮る
   縮小表示だと脚注が読めるかどうか判断できない。左端と右端の両方を見る
3. 領域ごとに「白でない画素の割合」を測り、どのブロックも0%でないことを確かめる
   （2360×1292、ヘッダー4.1% / 地図5.7% / 数字の帯6.1% / 脚注5.8%）

**検証用ブラウザのサンドボックスでは、`a.click()` のダウンロードがディスクに残らない。**
dataURL の生成と中身までは確認できるが、ファイル保存だけは実ブラウザで確かめる必要がある。

## 守っているもの

- 外から入る設定は必ず `normalizeConfig()` を通す（localStorage・JSONインポート）。
  壊れたJSON・配列・型違いを渡しても既定値に落ちる（`storage.test.ts`）。
- `seq` は保存する。要素数から復元しない。
- **計算できないものを 0 にしない。** `parseTimestamp` / `parseNumber` は `null` を返す。
  0 にすると滞留が実際より小さく出て、「思ったより悪くないですね」という逆効果の資料になる。
- CSVの中身は localStorage に入れない。保存されるのは設定だけ（実測 457 バイト）。
- 断定表現は `theme.ts` の `WORDING` に集約する。ここ以外に文言を書かない。

## レイアウト統合で決めたこと

- **外部システムとの対応づけは管理番号（`code`）だけで行う。**
  レイアウト側の内部ID（`i12`）は配置のたびに変わるのでキーにできない。
- **★名前が似ていても自動で紐づけない。** 工程名「表面処理」とレイアウト要素「表面処理」が
  完全一致でも、自動では結ばない。誤って結ぶと、誰も気づかないまま
  別の場所に金額が乗った図が出て行く。実ブラウザで 0/5 のままであることを確認済み。
- **対応が付いていない工程は描かず、件数と滞留を明示する。**
  「未対応の工程：5件 → そのため4本の工程間（平均2.1日の滞留）がこの図に出ていません。
  図だけを見ると実態より少なく見えます」と画面にも脚注にも出す。
- **レイアウトは「取り込んだ実データ」なので保存しない。** 保存するのは対応表だけ
  （`Config.processLayout`）。設定と観測を混ぜない。
- **`buildBands()` は App で1回だけ呼ぶ。** レイアウト画面と書き出し画像が同じ結果を読む。
- **位置の上に載せるのは「位置で判断が変わるもの」だけ。**
  載せたのは ①工程間の滞留の帯（太さ＝金額、色＝深刻度）②動線の長さ（m）×滞留＝
  「遠い＋待つ」が重なる区間。稼働率・不良率・時系列推移は載せない（表で見ればよい）。
- **座標変換と帯の形は `render/layoutShape.ts` だけが持つ。** SVG（画面）と Canvas（書き出し）が共有する。

## 数字の説得力（商談で使えないと言われた点）

**「総リードタイム／うち正味加工時間／うち滞留」の3つが足し算で合っていなかった。**
加工時間だけ標準時間から、滞留は実績から出しており、母数も違った。
実測で 0.3 + 8.5 = 8.8 ≠ 9.1。生産技術の人なら一目で気づく。

→ **加工など（実績）＝ リードタイム − 滞留** に変え、実績の時刻だけから出す。
これで必ず足し算が合う（`metrics.test.ts` で固定）。
標準時間は「内訳」ではなく**実績と比べる参考値**として別枠に出し、母数の件数も併記する。
「加工など」には検査・運搬・段取り・手待ちが含まれることを画面に明記する
（分離できないのに「正味加工時間」と名乗らない）。

**合成データの数字が現実離れしていた。** 標準時間を1工程30〜100分にしていたため、
数百個のロットなのに正味加工0.3日・滞留率98%という絵になっていた。
→ 1工程240〜420分（合計21稼働時間≒暦3日）に直し、滞留も控えめにした。

| | 修正前 | 修正後 |
| --- | --- | --- |
| 総リードタイム | 9.1日 | 8.7日 |
| うち加工など | 0.3日（足し算合わず） | 1.6日 |
| うち滞留 | 8.5日 | 7.1日 |
| 滞留率 | 93.2% | 81.3% |

**合成データで盛った数字を出さない。** 滞留率9割超の工場が無いわけではないが、
作った側が用意したデータでそれを出すと「盛った」と見られる。実データが来れば実態がそのまま出る。

## CSV解析が30倍遅かった（検証用データを大きくして初めて表に出た）

`parseCsv` が **1文字ずつ `field += ch` で継ぎ足していた**。
457万文字で **1,022 ms**。データ量に対して線形ではあるが、単純に遅い。

→ **区切りまで一気に `text.slice()` で切り出す**方式に変更。**31 ms**（33倍）。
引用符つきのフィールドだけ、`""` の位置で切り貼りする。
アプリ全体（読込→集計→描画）も 1,024 ms → 310 ms。

**11,885行のときは 91ms で「速い」と判断していた。** 行数を19,000に増やして初めて
表に出た。**性能は実データ規模で測る**という教訓が、そのまま自分に返ってきた。

### 性能テストの閾値は実測で決める

最初 200ms にしたが、**旧方式（簡略版）でも 167ms で通ってしまい、回帰を捕まえられなかった**。
実測して決め直した:

| | Node での実測 |
| --- | --- |
| 旧方式（1文字ずつ継ぎ足す） | 167 ms |
| 現方式（区切りまで切り出す） | 21〜54 ms |
| 閾値 | **100 ms** |

さらに1回だけの計測は GC に当たって落ちることがあったので、**3回測って最速を採る**。
5回連続で緑になることを確認した。

## 検証用データは3つの工場パターンで持つ

1つのデータだけで作ると、そのデータでしか動かないものが出来上がる。

| | 工程 | ロット | 滞留率 | 何を試すか |
| --- | --- | --- | --- | --- |
| case1 機械加工 | 8 | 2,377 | 82% | 主力。Shift_JIS版もある |
| case2 組立 | 4 | 900 | 65% | **健全な工場**。いつも問題ありと言う道具ではないこと |
| case3 複合加工 | 12 | 1,500 | 81% | 工程飛ばし・日付のみ8%・除外8%・重なり73件 |

**case2 が特に大事。** 滞留率65%・最大の工程間が0.8日という「まあまあ健全」なデータで、
アプリが騒がずに済むかを確かめられる。

## Shift_JIS 書き出しはスクリプト内で完結させた

Node は Shift_JIS を「読む」ことはできるが「書く」ことはできない。
そこで `TextDecoder('shift_jis')` で全2バイト組み合わせを1回デコードし、
**文字→バイトの逆引き表**を作って書き出す（23,436通り、一瞬で終わる）。
PowerShell に頼ると Git Bash の `pwd` が `/c/...` を返して .NET が誤解釈するなど、
環境差で何度もつまずいた。生成物はバイト単位で照合済み（読み戻して完全一致）。

## 画面の骨格：滞留マップを主役にする

**11枚のパネルを縦に全部並べたら、壁になって「どこを見ればいいか分からない」と言われた。**
機能は足りていたが、情報の出し方が悪かった。

- **読み込み前**は取り込み画面だけを大きく出す（パネル1枚）
- **読み込み後**は 細い上部バー（数字は3つだけ）→ **滞留マップ** → 押したものの詳細、の順
- それ以外（原価・書き出し・ロット一覧・レイアウト・品質・列の対応づけ）は
  **`Disclosure` で既定は閉じる**。見出しに「開かなくても分かる状態」を出しておく
  例:「原価を入れて金額を出す ／ 未設定（5品目）」「この数字の根拠 ／ 2,377件のうち2,124件から算出」
- 実測で画面の文字数が **710文字**まで減った（以前は数千文字が縦に並んでいた）

**地図は山も工程の箱も押せる。** 選択は `Selection`（工程 or 工程間）の1つの型にまとめ、
`SelectionDetail` が出し分ける。工程の詳細からは前後の滞留カードで隣に辿れ、
工程間の詳細からは両端の工程に辿れる。行き止まりを作らない。

例外として、**実績列が無くて計算できないときだけ**は診断を畳まずに出し、
「列の対応づけを直す」も最初から開く。ここで隠すと、
ユーザーが何を直せばいいのか分からなくなる。
